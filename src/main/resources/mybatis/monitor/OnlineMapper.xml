<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manage.project.monitor.online.mapper.UserOnlineMapper">

	<resultMap type="com.manage.project.monitor.online.domain.UserOnline" id="UserOnlineResult">
		<id     property="sessionId"         column="sessionId"         />
		<result property="loginName"         column="login_name"        />
		<result property="deptName"          column="dept_name"         />
		<result property="ipaddr"            column="ipaddr"            />
		<result property="longinLocation"    column="login_location"    />
		<result property="browser"           column="browser"           />
		<result property="os"                column="os"                />
		<result property="status"            column="status"            />
		<result property="startTimestamp"    column="start_timestamp"   />
		<result property="lastAccessTime"    column="last_access_time"  />
		<result property="expireTime"        column="expire_time"       />
		<association property="session" javaType="com.manage.project.monitor.online.domain.OnlineSession" resultMap="OnlineSessionResult" />
	</resultMap>
	
	<resultMap type="com.manage.project.monitor.online.domain.OnlineSession" id="OnlineSessionResult">
		<result property="host"              column="ipaddr"            />
		<result property="browser"           column="browser"           />
		<result property="os"                column="os"                />
		<result property="status"            column="status"            />
	</resultMap>
	
	<sql id="selectOnlineVo">
       select sessionId, login_name, dept_name, ipaddr, login_location, browser, os, status,  start_timestamp, last_access_time, expire_time 
	   from sys_user_online
    </sql>
    
	<select id="selectOnlineById" parameterType="String" resultMap="UserOnlineResult">
		<include refid="selectOnlineVo"/>
		where sessionid = #{sessionid}
	</select>

	<insert id="saveOnline" parameterType="com.manage.project.monitor.online.domain.UserOnline">
		replace into sys_user_online(sessionId, login_name, dept_name, ipaddr, login_location, browser, os, status, start_timestamp, last_access_time, expire_time)
        values (#{sessionId}, #{loginName}, #{deptName}, #{ipaddr}, #{longinLocation}, #{browser}, #{os}, #{status}, #{startTimestamp}, #{lastAccessTime}, #{expireTime})
	</insert>
	
 	<delete id="deleteOnlineById" parameterType="String">
 		delete from sys_user_online where sessionId = #{sessionId}
 	</delete>
 	
 	<select id="selectUserOnlineList" parameterType="com.manage.project.monitor.online.domain.UserOnline" resultMap="UserOnlineResult">
		<include refid="selectOnlineVo"/>
		<where>
			<if test="ipaddr != null and ipaddr != ''">
				AND ipaddr like concat('%', #{ipaddr}, '%')
			</if>
			<if test="loginName != null and loginName != ''">
				AND login_name like concat('%', #{loginName}, '%')
			</if>
		</where>
	</select>
	
	<select id="selectOnlineByExpired" parameterType="String" resultMap="UserOnlineResult">
		<include refid="selectOnlineVo"/> o 
		WHERE o.last_access_time <![CDATA[ <= ]]> #{lastAccessTime} ORDER BY o.last_access_time ASC
	</select>

	<update id="updateOnlineBySessionId"  parameterType="com.manage.project.monitor.online.domain.UserOnline">
		update sys_user_online 
	    <set>
	      <if test="loginName != null">
	        login_name = #{loginName},
	      </if>
	      <if test="deptName != null">
	        dept_name = #{deptName},
	      </if>
	      <if test="ipaddr != null">
	        ipaddr = #{ipaddr},
	      </if>
	      <if test="longinLocation != null">
	        login_location = #{longinLocation},
	      </if>
	      <if test="browser != null">
	        browser = #{browser},
	      </if>
	      <if test="os != null">
	        os = #{os},
	      </if>
	      <if test="status != null">
	        status = #{status},
	      </if>
	      <if test="startTimestamp != null">
	        start_timestamp = #{startTimestamp},
	      </if>
	      <if test="lastAccessTime != null">
	        last_access_time = #{lastAccessTime},
	      </if>
	      <if test="expireTime != null">
	        expire_time = #{expireTime},
	      </if>
	    </set>
	    where sessionId = #{sessionId}
	</update>
	
	<insert id="insert" parameterType="com.manage.project.monitor.online.domain.UserOnline">
		insert into sys_user_online(sessionId, login_name, dept_name, ipaddr, login_location, browser, os, status, start_timestamp, last_access_time, expire_time)
        values (#{sessionId}, #{loginName}, #{deptName}, #{ipaddr}, #{longinLocation}, #{browser}, #{os}, #{status}, #{startTimestamp}, #{lastAccessTime}, #{expireTime})
	</insert>
</mapper> 